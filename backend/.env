# =============================================================================
# Archivo de variables de entorno - Backend
# =============================================================================
# IMPORTANTE: Se sube archivo a git dado que es un proyecto universitario
# =============================================================================

# Configuración de la base de datos SQL Server
DB_SERVER=localhost
DB_PORT=1435
DB_DATABASE=gruas_heredianas
DB_USER=sa
DB_PASSWORD=GruasHer2026_Strong!

# Configuración del servidor
PORT=4000
NODE_ENV=development

# Clave secreta para firmar tokens JWT
# IMPORTANTE: Cambiar en producción por una clave más segura
JWT_SECRET=gruas_heredianas_jwt_secret_2026

# Tiempo de expiración del token JWT (en formato de la librería jsonwebtoken)
JWT_EXPIRES_IN=8h
